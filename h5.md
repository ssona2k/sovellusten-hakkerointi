## h5

### Lab2

File lacks debugging symbols to get the entire source code. 
We can still see list of the functions used in the program with command <code>info functions</code>.

There we see four interesting functions:
   - main
   - check_password
   - mAsdf3a
   - EaseEAs 

<img width="606" height="585" alt="image" src="https://github.com/user-attachments/assets/43027592-2f18-4425-9c68-a8baf1d5d138" />

With command <code>disas main</code> we can have a look into assembly code of main function. There we can have a look, what functions the program is calling during the run. What struck me, that function <code>check_password</code> wasn't called in the program.

It seems that <code>call mAsdf3a</code> is where the actual password check is happening.
The decision happens in lines <code>dec eax</code> and <code>jne</code>(jump if not equal). When the conditions are met, <code>call EaseEAs</code> will print the FLAG and grant access. 

<img width="922" height="1003" alt="Screenshot From 2026-09-22 23-25-32" src="https://github.com/user-attachments/assets/398693b9-fafc-44c8-831e-c5a62b17b4a3" />

Setting breakpoint to <code>main+133</code>. +133 refers to 133 bytes from the start of main (landing directly on the dec eax instruction). This is the line that checks the condition. 

<img width="741" height="107" alt="Screenshot From 2026-09-22 23-39-22" src="https://github.com/user-attachments/assets/e833bb12-e606-47a1-b553-5059e7893324" />

Let's run the program, and enter the wrong password.

<img width="1032" height="285" alt="Screenshot From 2026-09-22 23-52-37" src="https://github.com/user-attachments/assets/036bbbc6-5e9f-4ae1-9696-a84ddad5810f" />


The program stops on the line <code>0x5105</code>, that's just after the password input. 

<img width="1301" height="442" alt="Screenshot From 2026-09-22 23-50-04" src="https://github.com/user-attachments/assets/3af02911-6969-4df5-9953-f33509659f41" />

Because the password was invalid, function mAsdf3a sets <code>eax</code> memory slot to value -1. When program is stopped in debugger, we can manipulate value of the function program is trying to run in Assembly. In this case we change the value of <code>eax</code> memory slot to 1. This is the line that compares the user input to stored password, value 0 will not let the program to proceed, value 1 will proceed to flag.

<img width="1301" height="442" alt="Screenshot From 2026-09-22 23-50-04" src="https://github.com/user-attachments/assets/f4ffcba7-391b-439d-bae7-fd95dd3c83c5" />


